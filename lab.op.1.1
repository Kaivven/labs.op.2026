'use strict'

function inc1(n) {
    return n + 1;
}

const x = 123;
const y = inc1(x);
console.dir({x, y});


function inc2(num) {
    return num.n += 1;
}

const z = { n: 456 };
inc2(z);
console.dir(z);